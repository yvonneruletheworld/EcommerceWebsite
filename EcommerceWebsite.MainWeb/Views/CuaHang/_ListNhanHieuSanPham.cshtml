@using EcommerceWebsite.Data.Entities
@model List<NhanHieu>
<div class="category-sub-menu">
    <ul>
        @foreach (var item in Model.ToList())
        {
            <li>
                <a href="#" data-id="@item.MaHang" class="btn-hienthisanpham-hang">@item.TenHang</a>
            </li>
        }
    </ul>
</div>
<script>
  
</script>
